---
all:
  children:
    app_servers:
      hosts:
        app-prod:
          ansible_host: "YOUR_VM_IP"
          ansible_user: "root"
          ansible_password: "YOUR_SSH_PASSWORD"
          ansible_become_password: "YOUR_SUDO_PASSWORD"

