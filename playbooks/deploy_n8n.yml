---
- name: Deploy n8n queue mode
  hosts: "{{ target_hosts | default('all') }}"
  become: true
  gather_facts: true

  roles:
    - role: docker_host

  tasks:
    - name: Deploy n8n queue role
      ansible.builtin.include_role:
        name: apps/n8n_queue

